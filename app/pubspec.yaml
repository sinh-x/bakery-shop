name: bakery_app
description: "Bakery management mobile app for Tiệm Bánh Ninh Diêm"
publish_to: 'none'
version: 0.6.6+68

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State Management
  flutter_riverpod: ^3.1.0
  riverpod_annotation: ^4.0.0

  # Navigation
  go_router: ^17.2.3

  # Immutable Data Classes
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0

  # HTTP Client
  dio: ^5.7.0

  # Settings storage
  shared_preferences: ^2.5.5

  # Image picker
  image_picker: ^1.2.2

  # Cached network images for photo thumbnails
  cached_network_image: ^3.4.1

  # Device info (patched fork: remove KGP application)
  device_info_plus:
    git:
      url: https://github.com/sinh-x/plus_plugins.git
      path: packages/device_info_plus/device_info_plus
      ref: fix/built-in-kotlin
  package_info_plus:
    git:
      url: https://github.com/sinh-x/plus_plugins.git
      path: packages/package_info_plus/package_info_plus
      ref: fix/built-in-kotlin

  # Utilities
  uuid: ^4.5.1
  intl: ^0.20.2
  collection: ^1.19.1

  # Share & file (patched fork: remove KGP application)
  share_plus:
    git:
      url: https://github.com/sinh-x/plus_plugins.git
      path: packages/share_plus/share_plus
      ref: fix/built-in-kotlin
  path_provider: ^2.1.5

  # Permissions
  permission_handler: ^12.0.1

  # URL launcher (for tap-to-call)
  url_launcher: ^6.3.1

  # Printing (patched fork: remove KGP application)
  print_bluetooth_thermal:
    git:
      url: https://github.com/sinh-x/print_bluetooth_thermal.git
      ref: fix/built-in-kotlin
  image: ^4.8.0
  web: ^1.1.0
  gal: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linting
  flutter_lints: ^6.0.0

  # Code Generation
  build_runner: ^2.15.0
  freezed: ^3.2.3
  json_serializable: ^6.14.0
  riverpod_generator: ^4.0.0+1

dependency_overrides:
  shared_preferences_android:
    git:
      url: https://github.com/sinh-x/packages.git
      path: packages/shared_preferences/shared_preferences_android
      ref: fix/built-in-kotlin

flutter:
  uses-material-design: true
